# Database configuration
DATABASE_URL="postgresql://username:password@localhost:5432/dbname"

# NextAuth secret
# Generate a secure secret using the following command:
# openssl rand -base64 32
NEXTAUTH_SECRET="your-nextauth-secret"
